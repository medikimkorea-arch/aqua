{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "node server.ts",
  "env": {
    "NODE_ENV": "production"
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://localhost:3000/api/:path*"
    }
  ]
}
